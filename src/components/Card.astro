---
const { header, beskrivelse } = Astro.props;
---


<article>
  <h2>{header}</h2>
  <div class="carddesign">
    <slot />
  </div>
</article>

<article>
  <h2>{header}</h2>
  <div class="carddesign">
    <slot />
  </div>
</article>

<article>
  <h2>{header}</h2>
  <div class="carddesign">
    <slot />
  </div>
</article>

<style>
  article {
    border: 5px solid rgb(30, 30, 30);
    background-color: rgb(21, 21, 21);
    color: white;
    font-size: 14px;
    border: 2px solid rgb(30, 30, 30);
    border-radius: 10px;
    box-shadow: 0px 4px 8px rgba(0, 0, 0, 0.2);
  }
  p,
  .carddesign :global(p) {
    color: rgb(246, 246, 255);
  }

  .carddesign p {
    color: rgb(246, 246, 255); /* Ændre tekstfarve */
  }

</style>
